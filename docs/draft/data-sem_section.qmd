---
title: "Reciprocal relationships, reverse causality, and temporal ordering: Testing theories with cross-lagged panel models"
author:
  - Charles C. Lanfear
  - Thiago R. Oliveira
format: pdf
editor: source
execute:
  echo: false
  warning: false
  message: false
---

```{r setup}
# Just here to make sure it renders in R
```




```{tikz fig-2_classic-clpm, echo = FALSE, fig.cap = "Directed acyclic graph representation of classic CLPM"}
\usetikzlibrary{positioning}
\definecolor{black}{HTML}{000000}
\tikzset{
    > = stealth,
    every node/.append style = {
        draw = none
    },
    every path/.append style = {
        arrows = ->,
        draw = black,
        fill = none
    },
    hidden/.style = {
        draw = black,
        shape = circle,
        inner sep = 1pt
    }
}
\tikz{
    \node (y1) at (0,2) {$Y_1$};
    \node (y2) at (2,2) {$Y_2$};
    \node (y3) at (4,2) {$Y_3$};
    \node (x1) at (0,0) {$X_1$};
    \node (x2) at (2,0) {$X_2$};
    \node (x3) at (4,0) {$X_3$};
    \path (x1) edge (x2);
    \path (x2) edge (x3);
    \path (x1) edge (y2);
    \path (x2) edge (y3);
    \path (y1) edge (y2);
    \path (y2) edge (y3);
    \path (y1) edge (x2);
    \path (y2) edge (x3);
  }
```


```{tikz fig-3_contemporaneous-clpm, echo = FALSE, fig.cap = "CLPM with contemporaneous effects"}
\usetikzlibrary{positioning}
\definecolor{black}{HTML}{000000}
\tikzset{
    > = stealth,
    every node/.append style = {
        draw = none
    },
    every path/.append style = {
        arrows = ->,
        draw = black,
        fill = none
    },
    hidden/.style = {
        draw = black,
        shape = circle,
        inner sep = 1pt
    }
}
\tikz{
    \node (y1) at (0,2) {$Y_1$};
    \node (y2) at (2,2) {$Y_2$};
    \node (y3) at (4,2) {$Y_3$};
    \node (x1) at (0,0) {$X_1$};
    \node (x2) at (2,0) {$X_2$};
    \node (x3) at (4,0) {$X_3$};
    \path (x1) edge (x2);
    \path (x2) edge (x3);
    \path (x1) edge (y2);
    \path (x2) edge (y3);
    \path (y1) edge (y2);
    \path (y2) edge (y3);
    \path (y1) edge (x2);
    \path (y2) edge (x3);
    
    \path (x1) edge (y1);
    \path (x2) edge (y2);
    \path (x3) edge (y3);
  }
```



> CHUCK

## Imaginary data

* Reality and theory move at one recursive (really continuous) pace, but the practical limitations of empirical data collection result in snapshots of data---often captured at much lower resolution than our theory.
* For the purpose of illustration, let us assume that in both cases we have three waves of panel data with yearly waves of observation.
* The empirical observation periods clearly do not align with the theoretical causal process.
* Theory can imply whether you can plausibly isolate or rule out reciprocal effects or reverse causality.
* For our first example of the reciprocal effects of fear of crime and disorder perceptions, we expect that both processes of updating occur more rapidly than over course of a year---more quickly, even, than could be captured with *daily* data. Convincingly ruling out reciprocality would require biophysical measurements in a laboratory, an approach which would come at a cost of construct and external validity (i.e., real disorder out in the world).
* For our second example of reverse causality between employment and offending, we expect the process to occur less quickly but there is still plenty of time for feedback in yearly observations. If we decreasaed gaps between waves---perhaps to daily observations---we could convincingly rule out reverse causality. 
* More generally, we can imagine the discordance between our theoretical model and our empirical data collection as a missing data or latent variable problem as illustrated in FIGX.
* As noted, in the real world, time is continuous, so one might imagine there exists an infinite number of these "missing waves" between observations.
* One must always make some assumption about the way variables and relationships operate in these unobserved intervals.


> I need to think harder about this "missing data" thing about whether it alone really introduces the contemporaneous reciprocality thing or if this really only occurs in the context of demeaned variables / fixed effects.

```{tikz fig-time-dag-unobs, echo = FALSE, fig.cap = "A theoretical model of cross-lagged effects with incompletely observed periods."}
\usetikzlibrary{positioning}
\definecolor{black}{HTML}{000000}
\tikzset{
    > = stealth,
    every node/.append style = {
        draw = none
    },
    every path/.append style = {
        arrows = ->,
        draw = black,
        fill = none
    },
    hidden/.style = {
        draw = black,
        shape = circle,
        inner sep = 1pt
    }
}
\tikz{
    \node (y1) at (0,2) {$Y_1$};
    \node[hidden] (y15) at (2,2) {$Y_{1.5}$};
    \node (y2) at (4,2) {$Y_2$};
    \node[hidden] (y25) at (6,2) {$Y_{2.5}$};
    \node (y3) at (8,2) {$Y_3$};
    \node (x1) at (0,0) {$X_1$};
    \node[hidden] (x15) at (2,0) {$X_{1.5}$};
    \node (x2) at (4,0) {$X_2$};
    \node[hidden] (x25) at (6,0) {$X_{2.5}$};
    \node (x3) at (8,0) {$X_3$};
    \path (x1) edge (x15);
    \path (x15) edge (x2);
    \path (x2) edge (x25);
    \path (x25) edge (x3);
    \path (x1) edge (y15);
    \path (x15) edge (y2);
    \path (x2) edge (y25);
    \path (x25) edge (y3);
    \path (y1) edge (y15);
    \path (y15) edge (y2);
    \path (y2) edge (y25);
    \path (y25) edge (y3);
    \path (y1) edge (x15);
    \path (y15) edge (x2);
    \path (y2) edge (x25);
    \path (y25) edge (x3);
  }
```


```{tikz fig-time-dag-obs, echo = FALSE, fig.cap = "A theoretical model of cross-lagged effects with ambiguous contemporaneous relations."}
\usetikzlibrary{positioning}
\definecolor{black}{HTML}{000000}
\tikzset{
    > = stealth,
    every node/.append style = {
        draw = none
    },
    every path/.append style = {
        arrows = ->,
        draw = black,
        fill = none
    },
    hidden/.style = {
        draw = black,
        shape = circle,
        inner sep = 1pt
    }
}
\tikz{
    \node (y1) at (0,2) {$Y_1$};
    \node (y2) at (2,2) {$Y_2$};
    \node (y3) at (4,2) {$Y_3$};
    \node (x1) at (0,0) {$X_1$};
    \node (x2) at (2,0) {$X_2$};
    \node (x3) at (4,0) {$X_3$};
    
    \path (x1) edge (x2);
    \path (x2) edge (x3);
    
    \path (x1) edge (y2);
    \path (x2) edge (y3);
    
    \path (y1) edge (y2);
    \path (y2) edge (y3);
    
    \path (y1) edge (x2);
    \path (y2) edge (x3);
    
    \path (x1) edge [bend right=60, arrows = <->, dashed] (y1);
    \path (x2) edge [bend right=60, arrows = <->, dashed] (y2);
    \path (x3) edge [bend right=60, arrows = <->, dashed] (y3);
  }
```

## SEM diagrams

Data observed, and thus you must estimate, from discrete often arbitrarily spaced waves; this produces reverse causality

Once the theoretical model has been established, one can proceed to developing a statistical model that accounts for the constraints from the research design. Here, path diagrams from the structural equation modeling (SEM) tradition become useful.

* SEM diagrams encode both structural relations between variables indicated by single-headed arrows pointing from cause to effect and unanalyzed, often "nuisance" associations, indicated by double-headed arrows pointing between two variables. 
* Our recommendation is that the structural part of the SEM diagram should be recursive, corresponding as closely as possible to the theoretical model. Reciprocal simultaneous effects should generally be avoided as they require strong identifying assumptions, e.g., valid and relevant instrumental variables (Green 2003; XXXX).
* When the timing of observation results in ambiguity regarding the causal direction within any given time period---such as when periods $t=1.5$ and $t=2.5$ were not observed in Fig XXX---we can encode this ambiguity as an unanalyzed covariance.


  * Work out the diagrams; see if they are the same? Probably are!

FIGURE: Time estimator; the classic CLPM


* Explain the classic CLPM as an estimator

```{tikz fig-3-estimation, fig.cap = "SEM path diagram representation of three-wave CLPM"}
\usetikzlibrary{positioning}
\definecolor{black}{HTML}{000000}
\tikzset{
    > = stealth,
    every node/.append style = {
        draw = none
    },
    every path/.append style = {
        arrows = ->,
        draw = black,
        fill = none
    },
    hidden/.style = {
        draw = black,
        shape = circle,
        inner sep = 1pt
    }
}
\tikz{
    \node (x1) at (0,2) {$X_1$};
    \node (x2) at (2,2) {$X_2$};
    \node (x3) at (4,2) {$X_3$};
    \node (y1) at (0,0) {$Y_1$};
    \node (y2) at (2,0) {$Y_2$};
    \node (y3) at (4,0) {$Y_3$};
    \node (ex2) at (2.5,3) {$e_{x2}$};
    \node (ey2) at (2.5,-1) {$e_{y2}$};
    \node (ex3) at (4.5,3) {$e_{x3}$};
    \node (ey3) at (4.5,-1) {$e_{y3}$};
    \path (x1) edge (x2);
    \path (x2) edge (x3);
    \path (x1) edge (y2);
    \path (x2) edge (y3);
    \path (y1) edge (y2);
    \path (y2) edge (y3);
    \path (y1) edge (x2);
    \path (y2) edge (x3);
    \path (x1) edge [bend right=60, arrows = <->] (y1);
    \path (ex2) edge [bend left=45, arrows = <->] (ey2);
    \path (ex3) edge [bend left=45, arrows = <->] (ey3);
    \path (ex2) edge (x2);
    \path (ex3) edge (x3);
    \path (ey2) edge (y2);
    \path (ey3) edge (y3);
  }
```



> Thiago: Bollen & Pearl 2009 provide a nice discussion about the notational relationship between SEM path diagrams and DAGs.We should also look at Imai & Kim's three papers on causal inference with panel data. They use DAGs and they're so well written. I don't think they discuss reciprocal effects at all (could be wrong), but we could rely on them from a notational point of view in using DAGs to depict temporal processes.


# How it can go wrong!

* One para introducing the three key issues:
  * Temporal order and timing
  * Unobserved heterogeneity
  * Lack of inter-temporal variation